.link.link-back back

.content
	//img(src=image_l)
	.info
		h2= name
		- if (directed.length)
			ul.list.person-film-list Directed:
				each movie in directed
					li
						img(src=movie.image)
						span.link.link-film(data-id=movie.id)= movie.name
						span= ' (' + movie.imdb_rating + ') ' + movie.year
		- if (played.length)
			ul.list.person-film-list Played in:
				each movie in played
					li
						img(src=movie.image)
						span.link.link-film(data-id=movie.id)= movie.name
						span= ' (' + movie.imdb_rating + ') ' + movie.year
		article.description
			//p= desc

		div
			//a(href='http://www.imdb.com/title/tt#{imdb_id}', target='_blank') Full info on IMDB
